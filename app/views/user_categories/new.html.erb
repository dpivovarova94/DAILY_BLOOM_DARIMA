<h1>Which categories are you interested in?</h1>

<%= simple_form_for(@user_category) do |f| %>
  <%= f.input :category_id, collection:  Category.all.map { |category| [category.name, category.id]} , as: :radio_buttons, input_html: { multiple: true }%>
  <%= f.submit %>
<% end %>
